<h1 class="text-2xl font-bold mb-2"><%= @post.title %></h1>

<p class="text-sm text-gray-600">Minimum Age Required: <%= @post.min_age_required %></p>

<div class="mt-4 text-gray-800 leading-relaxed">
  <%= simple_format(@post.content) %>
</div>

<%= link_to "Back to Posts", organization_posts_path(@organization), class: "inline-block mt-4 text-blue-600 hover:underline" %>
